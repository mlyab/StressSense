# Core scientific stack
numpy>=1.26
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.13
PyYAML>=6.0

# Deep learning
# CPU build is enough for this project. If `pip install torch` is slow or pulls a
# GPU build you don't want, use the official CPU index:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.1

# Demo
gradio>=4.0
